# Optional MySQL configuration for importing and querying traceability data.
# Copy this file to .env and replace the placeholder values when needed.

ATE_DATA_SOURCE=auto
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=adapter_user
MYSQL_PASSWORD=change_me
MYSQL_DATABASE=adapter_ate
